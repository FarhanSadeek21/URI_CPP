#include <iostream>
using namespace std;
int main(){
    int testCase, number, type;
    cin >> testCase;
    for (int i = 0; i < testCase; i++){
        cin >> number >> type;
        if(t)
    }
    
}